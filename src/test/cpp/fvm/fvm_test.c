#include <fvm/fvm.h>

int main()  {
    printf("Hier");
    /*
    stack = (long*)malloc(100*sizeof(long));
    mem = (word*)malloc(1000*sizeof(word));
    w = (word*)malloc(sizeof(word));
    stptr = stack;
    ip = mem;

    addlit(100);
    addlit(200);
    *ip = &plus;
    ip++;
    void* ip_end = ip;

    ip = mem;
    
    // inner interpreter
    do {
        printf("start inner\n");
        *w = *ip;
        ip++;
        printf("call now\n");
        (*w)();
    } while (ip!=ip_end);

    printf("---->%ld\n", pop());
    */
}

